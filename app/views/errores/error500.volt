{{ content() }}
<div class="jumbotron">
    <h1>Error Interno</h1>
    <p>Ocurrió un error, si el error continua contacte al Administrador.</p>
    <p>{{ link_to('index', 'Inicio', 'class': 'btn btn-primary') }}</p>
</div>